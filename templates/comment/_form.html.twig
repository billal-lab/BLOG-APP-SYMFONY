<form class="d-flex" action="{{path('comment')}}" method="POST">
    <input class="form-control" type="text" name="comment">
    <input type="hidden" name="id" value="{{article.id}}">
    <button class="btn btn-primary" type="submit">Submit</button>
<form>